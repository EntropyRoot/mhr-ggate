# mhr-ggate development / test dependencies
# Install with:  pip install -r requirements-dev.txt
# This pulls runtime deps too (via the -r line below).

-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
